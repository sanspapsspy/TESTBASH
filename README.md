# TESTBASH - Установка

## Автоматическая установка

Запустите этот файл на Windows:
[install.bat](install.bat)

## Ручная установка

1. Скачайте репозиторий
2. Найдите файл TESTBASH.exe в папке `bin/`
3. Скопируйте его в `C:\Program Files\TESTBASH\`
4. Создайте ярлык на рабочем столе

## Для разработчиков

Сборка проекта:
```bash
dotnet build


4. Нажмите **"Commit changes"**

---

### **Шаг 3: Протестируйте установку**
Теперь любой пользователь может:
1. Зайти на https://github.com/sanspapsspy/TESTBASH
2. Найти файл `install.bat`
3. Нажать **"Raw"** → **Сохранить как** → `install.bat`
4. Запустить файл от администратора

---

## 💻 **АЛЬТЕРНАТИВА: Создать установщик локально**

Если хотите сначала протестировать на своем компьютере:

1. **Откройте Блокнот** (Notepad)
2. **Скопируйте код** установщика
3. **Сохраните как** `install.bat` (в папке с вашим проектом)
4. **Запустите** от имени администратора
5. Если работает — загрузите на GitHub

---

## 📌 **ВАЖНО: Убедитесь, что в репозитории есть .exe файл!**

Проверьте, есть ли в вашем репозитории скомпилированный `.exe` файл:
1. Зайдите на https://github.com/sanspapsspy/TESTBASH
2. Посмотрите структуру папок
3. Если нет `bin/Debug/` или `bin/Release/` с `.exe` файлом, то:

**Добавьте в репозиторий файл `build.bat`:**

```batch
@echo off
echo Сборка TESTBASH...
dotnet publish -c Release -r win-x64 --self-contained true
echo Готово! EXE файл в: bin\Release\net8.0-windows\win-x64\publish\
pause
